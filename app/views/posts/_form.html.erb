<%= form_with(model: post, data: {turbo: false })  do |form| %>
  <% if post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title, class: "input w-full  text-black" %>
  </div>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body, class: "textarea textarea-bordered text-black w-full" %>
  </div>

  <div class="my-5 flex space-x-3 justify-between items-center">
    <%= form.submit class: "btn btn-success"%>
    <%= link_to "Cencel", posts_path, class: "btn btn-info" %>
  </div>
<% end %>
